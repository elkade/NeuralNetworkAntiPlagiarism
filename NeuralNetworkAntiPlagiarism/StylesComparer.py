class StylesComparer(object):
    def __init__(self, clf):
        self.clf = clf
        pass

    def Train(X, y):
        clf.fit(X, y)
        pass

    def Calculate(X):
        result = clf.predict(X)
        return result
        pass